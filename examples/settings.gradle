pluginManagement {
    repositories {
        maven { // The google mirror is less flaky than mavenCentral()
            url "https://https://maven.aliyun.com/repository/public"
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'examples'
